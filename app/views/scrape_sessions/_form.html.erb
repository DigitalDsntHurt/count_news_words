<%= form_for(scrape_session) do |f| %>
  <% if scrape_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scrape_session.errors.count, "error") %> prohibited this scrape_session from being saved:</h2>

      <ul>
      <% scrape_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :scrape_date %>
    <%= f.date_select :scrape_date %>
  </div>

  <div class="field">
    <%= f.label :scrape_time %>
    <%= f.time_select :scrape_time %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
